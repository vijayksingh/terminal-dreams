"use client";

export function VideoStreamingDemo() {
  return (
    <div
      style={{
        padding: "var(--space-4)",
        fontFamily: "var(--font-mono)",
        fontSize: "var(--text-sm)",
        color: "var(--color-muted)",
        textAlign: "center",
      }}
    >
      Interactive lab available — click the lab button above to explore adaptive
      video streaming with ABR algorithms, buffer management, and bandwidth
      estimation.
    </div>
  );
}
