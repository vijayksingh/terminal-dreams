"use client";

export function WebPerformanceDemo() {
  return (
    <div
      style={{
        padding: "var(--space-4)",
        fontFamily: "var(--font-mono)",
        fontSize: "var(--text-sm)",
        color: "var(--color-muted)",
        textAlign: "center",
      }}
    >
      Interactive lab available — click the lab button above to explore web
      performance optimization with Core Web Vitals, resource waterfalls, and
      performance budgets.
    </div>
  );
}
